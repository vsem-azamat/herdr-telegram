# Keep the bot token outside the repository in a mode-0600 regular file.
BOT_TOKEN_FILE=/absolute/path/to/dev-bot.token

# For a private bot chat, Telegram normally uses the user's ID as the chat ID.
# The bridge still validates these as separate envelope fields.
ADMIN_ID=
CHAT_ID=
